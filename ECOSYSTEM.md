# IdeaPrism 세 서비스 — 한 몸으로 보기

**작성: 2026-08-11** · 어떤 작업을 시작하든 이 문서를 먼저 읽는다.

> **이 문서가 있는 이유**
>
> IdeaPrism은 서비스가 셋이지만 **따로 만드는 세 개의 프로그램이 아니다.**
> 하나를 고치면 다른 쪽이 흔들린다. 1.0의 표 하나를 바꾸면 2.0의 검색이 바뀌고,
> 2.0의 단계를 바꾸면 3.0의 화면 구조가 통째로 바뀐다.
>
> "이것만 살짝 고치면 되겠지"가 가장 위험하다. 고치기 전에 **5장 「영향 지도」**를 볼 것.

---

# 1. 한 문장으로

| | 이름 | 한 문장 |
|---|---|---|
| **1.0** | `ideaprism` | 학생이 **선배들의 발명을 찾아보는 곳** — 자료 창고 |
| **2.0** | `chatbot` | 학생이 **자기 발명을 만들어 가는 곳** — 대화가 조종석 |
| **3.0** | `dashboard` | 교사가 **100명을 한눈에 보고 짚어 주는 곳** — 관제탑 |

셋을 이어 붙이면 이렇게 읽힌다.

> **남의 발명을 보고(1.0) → 내 발명을 만들고(2.0) → 선생님이 지켜본다(3.0).**

---

# 2. 왜 하나인가 — 데이터가 한 줄로 흐른다

```
        ┌───────────────────────────────────────────────────┐
        │              Supabase (한 프로젝트)                 │
        │                                                   │
        │   inventions · grades · categories                │
        │   keyword_dictionary · ipc_descriptions           │  ← 1.0이 쌓은 자료
        │   ────────────────────────────────────            │
        │   invention_notes · prompt_overrides              │  ← 2.0이 쌓는 기록
        │   ────────────────────────────────────            │
        │   (앞으로) 교실·교사·처방                            │  ← 3.0이 쓸 것
        └───────────────────────────────────────────────────┘
              ▲                  ▲                  ▲
              │ 읽고 쓴다          │ 읽고 쓴다         │ 읽는다(주로)
              │                  │                  │
        ┌─────┴─────┐      ┌─────┴─────┐      ┌─────┴─────┐
        │    1.0    │─────▶│    2.0    │─────▶│    3.0    │
        │  자료 창고  │ 자료  │  발명 대화  │ 기록  │  교사 관제탑 │
        │           │ 검색식│           │      │           │
        └───────────┘ 이식  └───────────┘      └─────┬─────┘
                                   ▲                 │
                                   └─────────────────┘
                                      교사 처방이 되돌아온다
```

**핵심: 창고는 하나다.** 세 서비스가 같은 Supabase 프로젝트를 본다.
그래서 표 하나를 고치면 세 곳이 동시에 영향을 받는다.

---

# 3. 서비스별 상세

## 3.1 IdeaPrism 1.0 — 자료 창고

| | |
|---|---|
| **폴더** | `C:\Users\user\Documents\클맥_ideaprism` |
| **상태** | **운영 중.** 수정 금지 — 참고만 한다 |
| **누가** | 학생·교사 (파트너십 기관 코드로 입장) |
| **기술** | Next.js(App Router) · Supabase · KIPRIS |

**목적**

학생에게 KIPRIS(특허정보검색서비스)는 너무 어렵다. 그래서 **학생발명대회 수상작**을
학생 눈높이로 검색하게 만든 서비스다. "남들은 어떤 발명을 했나"를 보는 창.

**하는 일**

- 낱말로 수상작 검색 (한 번에 최대 500건을 브라우저 메모리에 적재)
- **SCAMPER 기법을 필터로** 활용 — 발상 도구를 검색 조건으로 쓴다는 것이 1.0의 발명
- 문제유형·학년·분야 필터
- 발명 하나를 골라 **KIPRIS 특허 검색식 자동 생성 → 실제 특허 조회**

**남기는 것 (2.0·3.0이 읽는 자산)**

| 표 | 내용 |
|---|---|
| `inventions` | 수상작 본문·태그·IPC — **모든 것의 뿌리** |
| `grades` · `categories` | 학년·분야 분류 |
| `keyword_dictionary` | 검색식에 쓸 낱말 사전 |
| `ipc_descriptions` | IPC 분류 설명 |
| `access_codes` 외 | 기관 인증 (지금은 1.0만 씀. **3.0의 교사 계정이 여기서 부활할 자리**) |

**디자인 (세 서비스의 얼굴 기준)**

- 무지개 워드마크 — I(빨) d(주) e(노) a(초) P(청록) r(파) i(남) s(보) m(자주)
- 프라이머리 블루 `#0066FF` · 바탕 slate `#F8FAFC` · 흰 카드 `rounded-2xl`
- 태그 색값(HEX)은 **한 글자도 바꾸지 않는다** — 아래 6장 참조

---

## 3.2 IdeaPrism 2.0 chatbot — 발명을 만드는 곳

| | |
|---|---|
| **폴더** | `C:\Users\user\Documents\클맥_ideaprism_chatbot` (지금 작업 중) |
| **상태** | 프로토타입 배포됨 · https://ideaprism-chatbot.vercel.app |
| **누가** | 학생 (익명 — 별명만) |
| **기술** | Next.js · Supabase(1.0과 **같은 프로젝트**) · Claude/OpenAI/Gemini |

**목적**

1.0이 "남의 발명을 보는 곳"이라면, 2.0은 **"내 발명을 만드는 곳"**이다.
AI 캐릭터(발명 선배·멘토)와의 대화가 곧 조종석이다. 학생이 채팅으로 검색을
조종하고, 발명 5단계를 완주하면 **발명노트가 저절로 완성된다.**

**하는 일**

- 세 캐릭터가 단계를 나눠 맡는다 — 발명 마스터 선생님(0단계) → 지유 선배(1~4단계) → 특허 탐정(5단계)
- AI는 **도구(리모컨) 8종**만 누른다. 화면을 바꾸거나 자료를 찾는 건 전부 도구
- 검색·필터·통계는 1.0의 자산을 이식해 그대로 쓴다
- 5단계에서 KIPRIS 선행기술조사 — **선배 발명의 IPC를 빌려** 검색식을 만든다
- 학생이 우측 화면을 직접 조작한다 (0~4단계). 5단계만 탐정이 주도

**남기는 것 (3.0이 읽을 유일한 데이터)**

`invention_notes` 표 한 장에 세션별로 쌓인다.

| 칸 | 내용 | 3.0에서 무엇이 되나 |
|---|---|---|
| `session_id` | 익명 세션 번호 | 학생 식별 (→ 계정 체계 필요) |
| `nickname` | 별명 | 카드 이름 |
| `current_stage` | 지금 몇 단계 | **칸반 열 위치** |
| `stages[N].artifact` | 그 단계의 최종 산출물 | 카드 본문 |
| `stages[N].earlierAttempts` | 고치기 전 기록 | 성장 이력 |
| `stages[N].dwellMs` | **그 단계에 머문 시간** | **병목 감지의 근거** |
| `stages[N].retries` | **완료 신청이 반려된 횟수** | **막힘 정도의 근거** |
| `kipris_query`·`kipris_summary` | 실제 조회한 검색식과 건수 | **진짜 선행특허 근거** |
| `ai_calls`·`token_usage` | 사용량 | 비용 관리 |

> **여기가 중요하다.** 3.0이 필요로 하는 "누가 어디서 막혔나"의 근거를
> **2.0이 이미 실제로 재고 있다.** 3.0은 새로 계측할 필요가 없다 — 읽기만 하면 된다.

**절대 깨뜨리면 안 되는 원칙** → 7장

---

## 3.3 IdeaPrism 3.0 dashboard — 교사 관제탑 (앞으로 만들 것)

| | |
|---|---|
| **폴더** | `C:\Users\user\Documents\클맥_ideaprism_dashboard` |
| **상태** | **화면 습작(UI 목업)만 있다.** 2.0보다 먼저 만든 것이라 **내용은 쓰지 않는다** |
| **누가** | 발명 지도교사 |

> **지금 폴더에 있는 것의 취급**
>
> Vite+React+Gemini로 만든 화면 습작이다. 학생 24명은 `src/initialData.ts`에
> 손으로 적힌 가짜 데이터이고, 브라우저 localStorage에만 저장된다.
> KIPRIS는 부르지 않고 AI에게 특허번호를 지어내게 한다. 3분 발표자료의
> "정체율 70% 감소" 같은 숫자도 근거가 없다.
>
> **그러므로 이 폴더는 "교사에게 어떤 화면이 필요한가"의 그림 자료로만 쓴다.**
> 코드와 데이터는 가져오지 않는다.

### 목적 (대표님 정의)

> **교사 한 명이 100명 넘는 발명 학생을 맡는다.**
> 그 교사가 학생들을 **효과적으로 파악하고 지도할 수 있게 돕는다.**

### 교사가 실제로 겪는 일

- 100명의 주제가 전부 다르다. 우산, 독서대, 칫솔, 킥보드… 한 명씩 봐 줄 시간이 없다
- 조용히 멈춘 학생은 티가 안 난다. **떠드는 학생만 챙기게 된다**
- 학생이 "이미 있는 특허예요"를 만나면 그 자리에서 포기해 버린다
- 학기 말에 누가 얼마나 성장했는지 설명할 자료가 없다

### 3.0이 해야 하는 네 가지

**① 파악 — 100명을 한 화면에**

발명 단계별 칸반. 어느 학생이 어느 단계에 있는지 한눈에.
1.0·2.0에서 이미 정한 단계를 그대로 쓴다. **3.0이 단계를 새로 정하지 않는다.**

**② 감지 — 조용히 멈춘 학생을 먼저 띄운다**

`dwellMs`(체류 시간)와 `retries`(반려 횟수)로 **프로그램이 판정한다.**
"AI가 보기에 막힌 것 같다"가 아니라 "3일째 2단계에 머물러 있고 완료 신청이 4번 반려됐다"는 **사실**.

**③ 이해 — 왜 막혔는지**

여기서만 AI를 쓴다. 대화 기록과 노트를 읽고 "무엇 때문에 막혔는지" 해석해 준다.
**숫자를 지어내지 않는다.** 특허 건수는 2.0이 실제로 조회한 값만 쓴다.

**④ 지도 — 짚어 준 것이 학생에게 닿는다**

교사가 힌트나 방향을 보내면 **2.0의 대화에 실제로 반영된다.**
캐릭터가 그 힌트를 자기 말로 학생에게 건넨다. (알림창이 아니라 진짜 전달)

### 3.0이 하지 말아야 할 것

- **단계를 새로 정의하지 않는다.** 단계는 2.0(과 시나리오 정의)에서 온다
- **숫자를 만들어 내지 않는다.** 세는 것은 프로그램, AI는 해석만
- **자체 데이터를 쌓지 않는다.** 학생 기록의 원본은 `invention_notes` 하나뿐
- **교사가 학생 대신 쓰지 않는다.** 발명은 학생 것이다

---

# 4. 서로 무엇을 주고받나

| 주는 쪽 | 받는 쪽 | 무엇을 | 어떻게 | 지금 상태 |
|---|---|---|---|---|
| 1.0 | 2.0 | `inventions`·`grades`·`categories` | 같은 Supabase | **작동 중** |
| 1.0 | 2.0 | `keyword_dictionary`·`ipc_descriptions` | 같은 Supabase | **작동 중** |
| 1.0 | 2.0 | KIPRIS 검색식 생성·조회 로직 | 코드 이식 | **작동 중** |
| 1.0 | 2.0 | 태그 색값·아이콘 | 코드 이식(`tag-styles.ts`) | **작동 중** |
| 1.0 | 2.0 | 무지개 워드마크·프라이머리 블루 | 디자인 토큰 이식 | **작동 중** (랜딩) |
| 2.0 | 3.0 | `invention_notes` 전체 | 같은 Supabase | **앞으로** |
| 2.0 | 3.0 | `classrooms`·`teachers` (교실·선생님) | 같은 Supabase | **표 준비됨** |
| 2.0 | 3.0 | `notes/summary.ts` 막힘 판정 | **코드 복사** (양쪽 함께 고칠 것) | **앞으로** |
| 3.0 | 2.0 | 교사 처방·힌트 | 새 표 + 턴 브리핑 주입 | **앞으로** |
| 1.0 | 3.0 | (간접) 발명 자료·IPC | 2.0을 거쳐 | **앞으로** |
| 1.0 | 3.0 | 기관·계정 인프라(`access_codes`) | 잠금 해제해 재사용 | **앞으로** |

---

# 5. 영향 지도 — 하나를 고치면 어디가 흔들리나

**고치기 전에 이 표를 볼 것.**

| 고치는 곳 | 흔들리는 곳 | 왜 |
|---|---|---|
| 1.0 `inventions` 표 구조 | **2.0 검색·카드·필터** → 3.0 통계 | 2.0이 같은 표를 직접 읽는다 |
| 1.0 태그 이름(문제유형·SCAMPER·분야) | **2.0 색·아이콘 판정** | 2.0은 낱말 포함으로 색을 고른다. 이름이 바뀌면 회색으로 떨어진다 |
| 1.0 태그 **색값(HEX)** | 2.0 카드 | 같은 자료가 두 서비스에서 다른 색이면 학생이 다른 자료로 본다 |
| 1.0 `keyword_dictionary`·`ipc_descriptions` | **2.0 5단계 검색식** | 2.0이 같은 표를 읽어 검색식을 만든다 |
| 1.0 KIPRIS API 사용법 | 2.0 특허 조회 | 같은 서비스키·같은 규격 |
| **2.0 발명 단계(`quest.ts` STAGES)** | **3.0 칸반 열 전체** | 3.0의 열이 곧 2.0의 단계다 |
| **2.0 `invention_notes` 구조** | **3.0 전부** | 3.0이 읽을 유일한 데이터 |
| 2.0 캐릭터 대본·`flow/` 지침 | 3.0 병목 해석 | 대본이 바뀌면 학생이 막히는 자리가 바뀐다 |
| 2.0 단계 완료 조건(`validate`) | 3.0 `retries` 의 의미 | 조건이 느슨해지면 병목 신호가 사라진다 |
| 3.0 교사 처방 | **2.0 대화 내용** | 처방이 캐릭터의 말에 끼어든다 |

**1.0을 고칠 때의 최소 확인 순서**

1. 2.0의 `npm test` — 태그·검색식 회귀 테스트가 1.0 규격을 못박아 두었다
2. 2.0 검색 화면에서 카드 색·필터가 그대로인지
3. 2.0 5단계에서 검색식이 만들어지고 KIPRIS 조회가 되는지

---

# 6. 세 서비스가 공유하는 원칙

이 원칙들은 **한 서비스의 규칙이 아니라 세 서비스의 계약**이다.

**① 숫자는 프로그램이 세고, AI는 해석만 한다**
검색 건수, 특허 건수, 체류 시간, 병목 판정 — 전부 코드가 계산한다.
AI가 목록에 없는 값을 지어내면 버리고, 무엇을 버렸는지 알린다.

**② 단계 판정은 코드가 한다**
AI가 "다음 단계로 가자"고 선언해도 넘어가지 않는다.
산출물의 "모양"이 아니라 **프로그램이 실제로 한 일**로 판정한다.
(AI는 하지 않은 조사를 그럴듯하게 적어 낸다 — 5단계에서 실제로 겪음)

**③ 자료의 원본은 하나다**
발명 자료는 `inventions`, 학생 기록은 `invention_notes`.
어느 서비스도 자기만의 사본을 따로 쌓지 않는다.

**④ 학생 개인정보는 최소로**
2.0은 별명만 받는다. 실명·학교·연락처를 묻지 않는다.
3.0에서 교사가 학생을 식별해야 하므로 **여기서 설계 결정이 필요하다** (8장 참조).

**⑤ 태그 색값은 고정**
1.0의 HEX를 한 글자도 바꾸지 않는다. 여기서 색을 "예쁘게" 고치면 두 서비스가 따로 놀기 시작한다.

**⑥ 얼굴은 하나**
무지개 워드마크, 프라이머리 블루 `#0066FF`, 둥근 필 버튼.
세 서비스를 오가는 학생·교사가 같은 서비스라고 느껴야 한다.

---

# 7. 용어를 통일하자

같은 것을 서로 다르게 부르고 있다. 문서·코드·화면에서 아래로 통일한다.

| 통일할 말 | 1.0에서 | 2.0에서 | 3.0(목업)에서 |
|---|---|---|---|
| **선배 발명** (수상작) | 발명품 | 선배 발명 | — |
| **학생 발명** (지금 만드는 것) | — | 발명노트 | 아이디어 |
| **단계** | (없음) | 단계 / stage | phase / 단계 |
| **선행기술조사** | 특허 검색 | 발명 / 특허검색 (5단계) | 선행조사 |
| **막힘** | — | 막힘 신호 | 병목 / 정체 |

---

# 8. 정한 것과 아직 안 정한 것

## ✅ ① 발명 단계 체계 — 데이터에서 나온다 (2026-08-12 결정)

**2.0이 단계를 데이터로 꺼냈다** (`src/lib/track/`). 단계 이름·개수·순서가
「트랙」 데이터에 있고, 트랙마다 다를 수 있다.

> **그러므로 3.0은 칸반 열을 코드에 박지 않는다.** 트랙 데이터에서 읽는다.
> 목업이 5단계(`문제발견`·`선행조사`·…)를 코드에 박아 둔 것이 바로 이 문제였다.

지금 있는 트랙은 「발명 5단계」 하나 — `0 시작 · 1 소재 발견 · 2 문제 정의 ·
3 문제해결(SCAMPER) · 4 아이디어 도출 · 5 발명/특허검색`.
「발명 리팩터링」·「대박특허 따라잡기」가 뒤따를 예정이고, **단계 수가 다를 것이다.**

## ✅ ② 학생과 교사를 잇는 법 — 교실 코드 (2026-08-12 결정)

**입장코드를 교실 코드로 승격한다.** 학생이 2.0 첫 화면에서 넣는 코드가 곧 교실이다.

- `classrooms.code` 하나가 교실 하나. 학생이 할 일은 늘지 않는다 (코드 하나 그대로)
- 노트에 `classroom_id` 가 찍히고, 그 반 선생님이 3.0에서 그 학생들만 본다
- **학생은 여전히 별명뿐이다.** 실명·학교·연락처는 안 받는다
- 선생님도 이메일·비밀번호가 아니라 **로그인 코드** 하나 (프로토타입 방식)

1.0의 `access_codes`·`partner_organizations` 재사용은 **하지 않았다** — 1.0을
건드리지 않고 새 표 둘로 끝냈다. 정식판 계정 체계에서 다시 볼 문제다.

## ✅ ③ 3.0은 별도 서비스 (2026-08-12 결정)

**따로 만든다.** 폴더 `클맥_ideaprism_dashboard`, 별도 저장소·별도 Vercel 프로젝트.
2.0과 같은 기술(Next.js)을 쓰고, **Supabase를 직접 읽는다** — 2.0에 기대지 않는다.
2.0이 멈춰도 선생님 화면은 살아 있어야 하기 때문이다.

대가: `notes/summary.ts` 의 막힘 판정이 **두 저장소에 복사**된다.
**한쪽 임계값을 고치면 다른 쪽도 고쳐야 한다.** MVP 동안의 빚으로 안고 간다.

## ④ 아직 — 학생 수 100명 규모에서의 성능

2.0은 지금 한 세션만 생각하고 만들었다.
100명치 `invention_notes`를 한 화면에 띄우는 것은 다른 문제다.

## ⑤ 아직 — 교사 처방이 학생에게 닿는 길

3.0에서 선생님이 쓴 힌트가 2.0 대화에 실제로 들어가는 부분(4장의 되돌아오는 화살표)은
아직 설계도 구현도 없다. MVP 범위 밖 — 노트가 선생님에게 닿는 것이 먼저다.

---

# 9. 지금 상태 요약

| | 1.0 | 2.0 | 3.0 |
|---|---|---|---|
| 상태 | **운영 중** | 프로토타입 배포됨 | 화면 습작만 |
| 손대도 되나 | **아니오** (참고만) | 예 (작업 중) | 다시 만든다 |
| Supabase | 씀 | 씀 (같은 프로젝트) | 아직 안 씀 |
| 인증 | 기관 코드 | 없음(익명) | 미정 |
| 회귀 테스트 | — | 94개 | — |

**함께 읽을 문서**

- [CLAUDE.md](CLAUDE.md) — 프로젝트 안내·일하는 방식
- [PRD.md](PRD.md) — 2.0 요구사항의 기준
- [HANDOFF.md](HANDOFF.md) — 2.0의 지금 상태와 다음 할 일, 함정 모음
