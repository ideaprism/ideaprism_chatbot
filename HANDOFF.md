# 인수인계 — 다음 세션에서 이어서 할 일

**갱신: 2026-08-12** · 새 대화창에서 작업을 이어받기 위한 문서.
[CLAUDE.md](CLAUDE.md) → [ECOSYSTEM.md](ECOSYSTEM.md) → [PRD.md](PRD.md) → 이 문서 순으로 읽으면 된다.

> **이 저장소는 세 서비스 중 하나다.** 1.0(자료 창고)·2.0(여기)·3.0(교사 관제탑)이
> 같은 Supabase를 본다. 무엇을 고치기 전에 [ECOSYSTEM.md](ECOSYSTEM.md) 5장
> 「영향 지도」에서 어디가 함께 흔들리는지 확인할 것.

**지금 상태: 로컬이 GitHub·Vercel(`16dab9a`)보다 앞서 있다.** 학습 프로그램 스튜디오
1단계(트랙으로 옮기기)를 로컬에 마쳤고 **아직 push 하지 않았다.**
회귀 테스트 133개 · typecheck · lint · build 전부 통과.

---

# 다음 할 일

## 1. 학습 프로그램 스튜디오 (가장 큼 — 설계 합의부터)

대표님이 원하는 것은 **"하드코딩된 프롬프트와 학습 흐름을 직접 제어하는 것"**이다.
관리자 페이지는 그 손잡이일 뿐이고, 진짜 일은 **학습 프로그램을 데이터로 꺼내는 것**이다.

**왜 필요한가 (대표님 말씀)**

- 앞으로 「발명 리팩터링」, 「대박특허 따라잡기」 같은 콘텐츠를 더 만든다
- AI 회사별 프롬프트 튜닝을 수시로 한다
- 모듈처럼 갈아 끼우고, 조합해서 여러 track을 만든다
- **버전 관리**가 필요하다
- 처음에 학생에게 선택지를 준다 (선행조사까지 해 본 학생을 소재발견부터 밟게 할 필요 없다)

**✅ 1단계는 끝났다** (아래 「만드는 순서」 표의 1번). 지금 상태:

- 단계 이름·대본·도구·완료 조건이 전부 **`lib/track/tracks/invention5.ts` 한 파일의 데이터**다
- `lib/quest.ts` 는 이제 그 데이터를 읽어 "넘어가도 되는가"만 판정한다
- 세션이 **`quest.trackId` 로 트랙을 붙든다** (없으면 기본 트랙 — 옛 대화도 이어진다)
- 단계 수가 고정이 아니다. `StageId` 는 `number` 이고, 단계 목록은 트랙만이 안다

**완료 조건 — 부품 서랍 (`lib/track/rules.ts`)**

대표님 결정: **부품 서랍 + 자유 문장.** 지금 만들어 둔 부품 아홉 종류:

| 부품 | 무엇을 보는가 |
|---|---|
| 글이 있다 | 그 칸에 몇 글자 이상 |
| 목록에 N개 이상 | 개수 + 항목마다 최소 길이 |
| 사람을 골랐다 | 정해진 목록에서 서로 다른 N명 |
| 실제로 조회했다 | **프로그램이 관측한 사실** — AI 주장 아님 |
| 화면의 것과 같다 | 적어 낸 값 vs 화면에 뜬 값 |
| 0건이면 비운다 | 안 나온 것을 적지 못하게 |
| 나왔으면 적는다 | 나온 것을 빠뜨리지 못하게 |
| 선배 발명을 골랐다 | 무엇을 기초로 삼았는가 |
| **자유 문장 (AI 판정)** | 부품으로 표현 못 하는 것 |

**자유 문장에 건 안전장치 두 개** (원칙 2를 지키려고):

1. **부품을 전부 통과한 뒤에만 자유 문장을 묻는다.** 자유 문장 하나만으로는 절대 못 넘어간다
2. **판정 결과가 없으면 통과시키지 않는다(fail closed).** "못 물어봤으니 그냥 통과"는
   AI가 안 한 일을 했다고 적는 것과 같은 구멍이다

> ⚠️ **자유 문장을 AI에게 실제로 물어보는 기계는 아직 안 붙였다.**
> 관리자에서 문장을 적을 수 있게 되는 **3단계에서 붙인다.** 지금 「발명 5단계」는
> 자유 문장을 하나도 안 쓰므로 아무 영향이 없다(테스트로 못박아 뒀다).
> **3단계에서 이걸 빼먹으면, 대표님이 문장을 적는 순간 학생이 그 단계에 갇힌다.**
> 붙일 자리: `judgeQuestions(rules)` 로 질문을 꺼내 AI에게 묻고,
> 그 답을 `advanceStage(..., judgments)` 7번째 인자로 넘기면 된다.

**세 콘텐츠는 모양이 다르다**

| | 입력 | 학생이 하는 일 | 정답 |
|---|---|---|---|
| 발명 5단계 | 없음 | 자기 발명을 만든다 | 없음 |
| 발명 리팩터링 | **기존 발명 하나** | 그걸 개선한다 | 없음 |
| 대박특허 따라잡기 | **문제 + 힌트 사다리** | 도전한다 | **있다** |

「따라잡기」의 정답 판정 — **제안: 정답으로 막지 않는다.** 승급은 "N번 시도했고 최종안을
적었다"로 코드가 판정하고, 정답과의 거리는 AI가 점수만 보여 준다. 틀려도 배우는 게 목적이다.

**놓치기 쉬운 함정 — 건너뛰기는 공짜가 아니다**

5단계 특허 검색은 4단계 산출물을 재료로 쓴다. 그냥 뛰어넘으면 재료 없이 시작한다.
→ **「인계 모듈」**: 건너뛰기가 허용되는 지점마다 캐릭터가 3~4마디로 필요한 것만 받아 적는다.
모듈이 "필요한 것"을 스스로 선언하게 하면 프로그램이 빠진 것만 골라 묻는다.

**버전 관리 — 제안**

> **발행본은 못 바꾸고, 세션이 버전을 붙든다.**
> 고치는 중(초안) → [발행] → v3(영원히 안 바뀜). 대화 중인 학생은 v2로 끝까지 간다.
> 안 그러면 완료 조건이 바뀌는 순간 **이미 통과한 학생이 소급해서 미달**이 된다.
> 발명노트에 어떤 버전으로 했는지도 남겨야 한다(3.0이 해석할 수 있게).

**AI 회사별 프롬프트 — 제안**

어디든 `기본` + `회사별 덧붙임` 두 겹으로. 관리자에서 탭으로 넘기며 편집하고,
비어 있으면 기본을 쓴다. "수시로 튜닝"하려면 **같은 입력에 두 버전을 넣어 나란히 보는 비교
화면**이 결국 필요하다 — 지금 데이터 구조를 그걸 할 수 있게 만들어 둘 것.

**n8n 스타일 편집 화면 (대표님이 1.0에서 시도했던 것)**

1.0의 `src/components/admin/WorkflowCanvas.tsx` + `SettingsPanel.tsx` 를 볼 것.
React Flow(`@xyflow/react`)로 파이프라인을 그리고 **노드를 클릭하면 그 지점의 모델과
프롬프트를 편집** → `bot_settings` 표에 저장. 2.0에도 같은 라이브러리로 얹는다.
캔버스에 그릴 것: 전역(공통규칙·도구 설명·모델) + 단계별 노드(할 일·완료 조건·도구·담당).

**만드는 순서 (합의)**

| | 무엇 | 눈에 보이는 것 | |
|---|---|---|---|
| 1 | 지금 5단계를 **"발명 5단계" 트랙 하나로** 옮긴다 + 검증 부품 서랍 | **아무것도 안 바뀜** ← 이게 성공 기준 | **✅ 끝** |
| 2 | 랜딩에서 **트랙 고르기 + 시작 지점 고르기** + 인계 모듈 | 학생이 고른다 | ← 다음 |
| 3 | 관리자에서 **모듈·트랙 편집 + 버전 발행** (+ 자유 문장 판정기) | 대표님이 직접 만진다 | |
| 4 | **AI 회사별 프롬프트 보정** + 비교 | 튜닝이 된다 | |
| 5 | **발명 리팩터링 · 대박특허 따라잡기** | 새 콘텐츠 | |

**2단계에서 할 일 (지금 그릇은 다 있다)**

- `Track.input` 이 비어 있다 — 「리팩터링」의 "고쳐 볼 발명 하나", 「따라잡기」의 "문제+힌트"가
  들어갈 자리. 타입만 있고 화면·수집 절차가 없다
- **시작 지점 고르기와 「인계 모듈」이 아직 없다.** 5단계는 4단계 산출물을 재료로 쓰므로
  그냥 뛰어넘으면 재료 없이 시작한다 (아래 「건너뛰기는 공짜가 아니다」)
- `createSession()` 과 랜딩이 트랙을 고르지 않는다 — 늘 기본 트랙으로 시작한다

**대표님이 정해 주신 것 (2026-08-12)**

| 물음 | 답 |
|---|---|
| 트랙을 담는 그릇의 넓이 | **셋 다 담기게, 만들기는 하나만** |
| 트랙마다 단계 수가 다른가 | **자유롭게** (단계 수·이름·순서 전부) |
| 완료 조건을 관리자에서 조립하나 | **부품 서랍 + 자유 문장** |

> 「단계 수 자유」의 대가: **3.0 교사 대시보드의 칸반 열이 트랙마다 달라진다.**
> 3.0은 열 목록을 코드에 박지 말고 트랙 데이터에서 읽어야 한다 (ECOSYSTEM 8장 ①이 여기서 풀린다).

## 2. 선생님 supervisor — 안 불러도 끼어든다

대표님 그림: *"선생님은 개입 안 하더라도 다 지켜보고 있는 거고, 안 불러도 개입이 가능한 supervisor"*

지금은 **전문가 초대(`call_expert`)만** 있다. 누가 불러야 온다.
선생님이 스스로 끼어드는 기능은 없다.

> **제안: 코드가 신호를 세고, 걸리면 선생님을 부른다.**
> 매 턴 감시용 AI를 따로 돌리면 **비용이 두 배**가 된다.
> 이미 세고 있는 신호가 있다 — 주제 이탈 3회(`offTopicCount`), 완료 반려 횟수(`quest.retries`),
> 단계 체류 시간(`quest.enteredAt`). `lib/notes/summary.ts` 의 막힘 판정과 같은 재료다.
> 기준값은 관리자에서 조절하게 만든다. AI 감시자는 이게 부족할 때 붙인다.

## 3. 관리자 페이지 — 남은 것

탭 셋(프롬프트 · 이용내역 · 점검)까지는 만들었다.

- **시나리오 관리** — 1번이 끝나야 붙일 수 있다
- **설정** — 지금 코드에 박혀 있는 숫자들을 대표님이 조절하게.
  후보: `lib/notes/summary.ts` 의 막힘 임계값(3회·30분·2일),
  `lib/ai/config.ts` 의 모델 이름·세션당 호출 상한

## 4. 작은 것들 (언제든)

- **학생이 답변 도중에 화면을 벗어나면 서버가 오류를 남긴다.** 이미 닫힌 통로에
  계속 글을 밀어 넣는다 (`route.ts` 의 `send` → `controller.enqueue`).
  로그에는 `openai 응답 실패 ... Controller is already closed` 로 찍히는데
  **AI 잘못이 아니라 학생이 나간 것**이라 원인을 오해하기 쉽다.
  화면에는 안 보이지만 나간 뒤에도 토큰을 계속 쓴다.
  고칠 자리: 통로가 닫혔으면 `send` 를 조용히 넘기고, 학생이 없으면 AI 턴을 일찍 끊는다.
  (스트림 뒤에 도는 노트 저장이 함께 건너뛰어지지 않는지 확인할 것)
  → 트랙 옮기기와 무관한 **원래 있던 문제**다 (2026-08-12 실측)
- **감정 그림이 휴대폰(375px)에서 랜딩 가운데를 스크롤시킨다.** 10명 소개를 좁은 폭에
  다 담기엔 좁다. 소개 줄을 빼거나 2열로 만들면 없앨 수 있다 (대표님 판단 필요)
- **0단계 「스케치」 캐릭터** — 페르소나 문서에서 언급은 지웠다. 만들 계획이면 대본+그림 10장 필요
- **`personas/master_teacher.txt:54`** — "대화의 끝은 항상 '그 다음은 어떻게 될까?'"가
  박혀 있어 매번 그 말이 붙는다. 거슬리면 지울 것

---

# 대표님이 직접 하셔야 할 것 (아직 남음)

1. **사이트가 인증 없이 열린다.** 주소를 아는 사람은 누구나 대화할 수 있고
   **AI 비용이 대표님 계정에서 나간다.** 콘솔 월 한도를 걸거나
   Vercel Deployment Protection을 Standard로 올릴 것.
2. **Claude 키만 비어 있다** (`ANTHROPIC_API_KEY`). OpenAI(`gpt-5.4-mini`)·Gemini(`gemini-3.6-flash`)는 작동 확인됨.
3. **관리자 로그인 뒤 화면은 미확인.** AI가 비밀번호를 다루지 않아 못 들어간다.
   `배포주소/admin` 에서 ① 대화구조 바꿔 저장 ② 이용내역 탭 확인 — 이 둘만 봐 주시면 된다.

---

# 끝난 일 (2026-08-12 세션)

## ✅ 학습 프로그램을 데이터로 꺼냈다 (스튜디오 1단계)

네 곳에 흩어져 박혀 있던 「발명 5단계」를 **트랙 데이터 한 덩어리**로 모았다.
`quest.ts` 의 STAGES · `flow.ts` 의 파일 이름표 · `tools.ts` 의 STAGE_TOOLS · 완료 조건 함수.

**화면은 한 군데도 안 바뀌었다** — 그게 성공 기준이었다. 회귀 테스트 112개가
**한 줄도 고치지 않고** 그대로 통과한다. 부품 서랍용 테스트 21개를 새로 얹어 133개.

| 새 파일 | 무엇 |
|---|---|
| `lib/track/types.ts` | 트랙·단계의 그릇 (셋 다 담게) |
| `lib/track/rules.ts` | **검증 부품 서랍** + 판정기 |
| `lib/track/tracks/invention5.ts` | 「발명 5단계」 — 옮긴 데이터 |
| `lib/track/index.ts` | 트랙 보관함 (모르는 이름은 기본 트랙으로) |

**옮기면서 지킨 것**: 반려 안내 문구를 한 글자도 안 바꿨다. 5단계의 판정 **순서**도
그대로다 — 조회 안 했으면 거기서 멈추고(`stop`), "검색식이 다르다"는 말을 안 한다.

## ✅ 감정 그림이 문단마다 바뀐다

원인이 셋이었다 — 서버가 첫 감정만 내보냄 / 파서가 "글의 어디에서"를 잃음 /
대본이 "시작만" 요구함. 파서가 순서를 지켜 돌려주고(`ParsedPart[]`),
브라우저가 자리를 적어 두고(`EmotionMark`), 화면이 토막마다 그림을 붙인다.
**그림은 그 대사와 함께 나온다** (글자 없는 토막은 안 내보낸다).

## ✅ 랜딩페이지 — `/` 랜딩, 대화는 `/chat`

1.0 디자인 규칙. 스크롤 없이 한 화면. "이어서 하기" 지원.

## ✅ ECOSYSTEM.md — 세 서비스는 한 몸이다

목적·연결·**영향 지도**(무엇을 고치면 어디가 흔들리나)·공유 원칙·용어 통일·열린 질문.

## ✅ 페르소나 열 명 전원 투입

교사 1 · 학생 선배 6 · 전문가 3. 감정 그림 100장을 GitHub와 전수 대조(어긋남 0).
랜딩에 세 갈래로 소개. 지원은 얼굴 대신 물음표 + "기업 연구원".

## ✅ 대화구조를 관리자에서 손댄다

단계별 담당을 `cast.ts` 로 꺼냈다. 세션이 배치를 **붙든다**(도중에 바뀌어도 끝까지 유지).
완료 조건은 여전히 코드가 판정한다.

## ✅ 헤더 네비게이션 + 단계 이름 갱신

로고를 랜딩과 같게(`Wordmark`). 단계를 한 줄로.
`0 시작 · 1 소재 발견 · 2 문제 정의 · 3 문제해결(SCAMPER) · 4 아이디어 도출 · 5 발명 / 특허검색`

## ✅ 선생님이 친구 둘을 짝지어 주고, 둘이 함께 대화한다

0단계에서 대화해 보고 학년·성향에 맞는 **친구 두 명**을 고른다. 5단계 흐름도 알려 준다.
`[말:id]` 표식으로 **둘이 실제로 번갈아 말한다** (각자 얼굴·이름·색).

## ✅ 전문가 초대 (`call_expert` · `send_off_expert`)

탐정은 5단계에서 친구가 부른다. 코치·연구원은 부를 때만. 한 번에 한 명, 단계가 바뀌면 돌아간다.

## ✅ 관리자 페이지 — 탭 셋

프롬프트 · **이용내역**(막힘 신호·단계별 기록·토큰) · **점검**(`/api/health` 를 화면으로).

---

# 지금 상태

## 배포

| | |
|---|---|
| GitHub | https://github.com/ideaprism/ideaprism_chatbot (**공개**) |
| Vercel | https://ideaprism-chatbot.vercel.app · 프로젝트 `ideaprism-chatbot` |
| 배포 커밋 | `16dab9a` — **로컬이 앞서 있다.** 트랙 옮기기는 아직 push 안 함 |

**배포본에서 실제로 확인한 것**: 랜딩(10명 3그룹) → 첫 인사 스트리밍 →
선생님이 친구 둘 매칭 → 둘이 번갈아 대화 → 전문가 초대·배웅 → 관리자 잠금(401).
런타임 오류 0건. `/api/health` 가 페르소나 10명·flow 9개를 모두 읽는다.

## 폴더 구조

```
ECOSYSTEM.md  세 서비스(1.0·2.0·3.0)의 목적·연결·영향 지도   ← 작업 전 필독
personas/     캐릭터 대본 10개 (공장 초기값) — /admin 에서도 고침
flow/         대화 흐름 지침 9개 (공장 초기값)
supabase/     invention_notes · prompt_overrides DDL (둘 다 적용됨)
tests/        회귀 테스트 133개 (core · track · notes · usage · kipris · search …)
src/app/
  page.tsx            랜딩 (1.0 디자인 토큰, 10명 3그룹)
  chat/page.tsx       채팅 + 2패널
  admin/page.tsx      관리자 껍데기(Console) + 프롬프트 탭
  admin/UsageTab.tsx  이용내역
  admin/HealthTab.tsx 점검
  api/                chat · cast · search · kipris · ipc · invention-keywords
                      admin/{session,prompts,notes} · health · providers
src/components/
  Wordmark.tsx      무지개 로고 (랜딩·대화 화면 공용)
  MessageList.tsx   말풍선 — 사람·감정이 바뀐 자리에서 토막
src/lib/
  track/            학습 프로그램(트랙) — 단계의 "내용"은 전부 여기 데이터
    types.ts          트랙·단계의 그릇 (단계 수 자유, 시작 재료 자리)
    rules.ts          검증 부품 서랍 + 판정기   ← 완료 조건은 여기서 조립
    index.ts          트랙 보관함 (getTrack · stageAt · stageIdsOf)
    tracks/
      invention5.ts   「발명 5단계」 — 이름·대본·도구·완료 조건
  cast.ts           대화구조 — 어느 단계에 누가 (순수 함수)
  characters.ts     캐릭터 10명 + 감정 그림 매핑
  emotion.ts        말풍선 토막내기 splitSpeech (순수 함수)
  quest.ts          퀘스트 상태기계   ← "넘어가도 되는가"만. 단계 내용은 track/
  tools.ts          도구 10종 명세 (단계별 목록은 트랙이 정한다)
  tool-handlers.ts  도구 실행기
  prompt.ts         프롬프트 조립 + 표식 파서 + 여럿이 말하기 규칙
  notes/summary.ts  이용내역 요약 + 막힘 신호 (순수 함수) ← 3.0이 그대로 쓸 값
  prompts/store.ts  프롬프트·설정 저장소 (파일/코드 기본값 + DB 덮어쓰기)
  admin/auth.ts     관리자 잠금 (접근 코드·서명 쿠키)
  personas.ts       페르소나 정제 (순수 함수 — server-only 들이지 말 것)
  tag-styles.ts     태그 색·아이콘 (1.0과 같은 색값)
  search/ kipris/ notes/ ai/
```

---

# 절대 깨뜨리면 안 되는 것 (PRD 아키텍처 원칙)

1. **AI는 주어진 도구만 누른다** (지금 10종). 미구현 도구는 AI에게 전달조차 하지 않는다.
2. **단계 판정은 코드가 한다.** `complete_stage` + `quest.ts` 검증을 통과해야만 승급.
   산출물의 "모양"만 보지 말고 **프로그램이 실제로 한 일**(`StageEvidence`)로 판정할 것 —
   AI는 하지 않은 조사를 그럴듯하게 적어 낸다(5단계에서 실제로 겪음).
3. **감정·화자는 이름만.** AI는 `[감정:이름]`·`[말:id]` 표식만 쓰고, 그림 주소와 화면 배치는 코드가.
4. **숫자는 프로그램이 센다.** AI가 목록에 없는 값을 지어내면 버리고 무엇을 버렸는지 알린다.
5. **대화 도중 모델 회사 교체 금지.**
6. **개인정보 미수집.** 별명만 받는다.

## 세 회사가 공통으로 요구하는 것

도구를 호출한 모델 턴을 되돌려 줄 때 **회사마다 이름만 다른 "생각 흔적"을 살려야 한다.**
안 그러면 다음 턴이 400으로 막힌다. 지금은 `AiMessage.raw` 로 셋 다 해결돼 있다.

| 회사 | 살려야 하는 것 |
|---|---|
| Claude | thinking 블록 |
| OpenAI | reasoning 항목 (그래서 Chat Completions 대신 **Responses API**) |
| Gemini | `thoughtSignature` |

---

# 굳힌 결정 (다시 고민하지 말 것)

## 학습 프로그램 (트랙)

- **단계의 "내용"은 `track/` 데이터, "판정"은 `quest.ts`.** 새 학습 프로그램을 만들 때
  `quest.ts` 를 고치게 됐다면 뭔가 잘못 짠 것이다.
- **단계 수를 고정하지 않는다.** `StageId` 는 `number` 이고, 어떤 번호가 있는지는
  그 트랙의 `stageIdsOf()` 만 안다. **`stage < 5`·`0~5` 같은 걸 새로 쓰지 말 것** —
  `stageIdsOf` 의 순서로 세면 번호가 띄엄띄엄이어도 맞는다.
- **모르는 트랙 이름·모르는 단계 번호는 조용히 기본값으로 되돌린다.** 트랙이라는 개념이
  없던 시절에 브라우저에 저장된 대화가 남아 있기 때문이다 (`quest.trackId` 는 없어도 된다).
- **자유 문장(AI 판정)은 부품을 다 통과한 뒤에만, 판정 결과가 있을 때만 통과.**
  이 두 줄이 원칙 2를 지키는 전부다. 느슨하게 풀면 AI가 단계를 스스로 올리게 된다.
- **`quest.ts` 는 여전히 서버 전용 코드를 들이지 않는다.** 브라우저(진행판·발명노트)가
  같은 값을 읽는다. `track/` 도 마찬가지다 — `server-only` 를 들이면 회귀 테스트가 깨진다.
- ⚠️ **`notes/summary.ts` 는 아직 기본 트랙 기준이다.** 저장된 발명노트를 관리자 화면이
  읽을 때 단계 이름을 기본 트랙에서 가져온다(기록 안에 `label` 이 있으면 그것을 우선 쓴다).
  트랙이 둘 이상이 되는 **2단계에서 함께 손봐야 한다** — 3.0 칸반도 같은 값을 쓴다.

## 누가 함께 있는가 (대화구조)

- **0단계 선생님이 학생과 이야기해 보고 발명반 친구 두 명을 짝지어 준다.**
  그 둘이 **1~5단계를 끝까지 함께 간다.** 짝은 코드가 검증한다 —
  선배 목록에 있는 서로 다른 둘이 아니면 승급이 막힌다.
- **선생님은 5단계 흐름을 짧게 알려 준 뒤 친구를 소개한다.** (`flow/0-시작.md`)
- **둘이 함께 말한다.** AI가 `[말:id]` 표식을 찍고, 화면이 그 자리에서 말풍선을 갈라
  각자의 얼굴·이름·색을 붙인다. 감정 토막내기와 같은 기계를 쓴다.
  **화자가 바뀌면 감정은 그 사람의 기본 감정에서 다시 시작한다** (감정 이름이 사람마다 다르다).
- **전문가는 자리를 차지하지 않고 불려 오는 손님이다** (`call_expert`·`send_off_expert`).
  - **특허 탐정** — 5단계에서 친구가 바로 부른다. 찾는 일은 탐정이 주도하고,
    친구는 남아서 어려운 말을 학생 눈높이로 풀어 준다.
  - **사업 코치·기업 연구원** — 학생이 원하거나 정말 필요할 때만.
  - 한 번에 한 명. 단계가 바뀌면 저절로 돌아간다.
  - 손님을 부른 **그 턴부터** 바로 말할 수 있다(`route.ts` 의 `liveVoices`).
    대본은 다음 턴부터 들어오므로 첫 인사만 즉흥이다.
- **부를 수 있는 사람은 전문가 셋뿐이다.** 목록에 없는 이름의 `[말:]` 표식은 화면이 버린다.
- 관리자에서는 단계마다 **[이끄는 사람] [함께]** 두 칸으로 고른다.
  다만 **1~5단계는 선생님이 고른 둘이 덮어쓴다** — 관리자 설정은 그 전까지의 기본값이다.

## 감정 그림

- **한 말풍선을 감정이 바뀐 자리에서 토막 내고, 토막마다 그림을 붙인다.**
  (대표님이 세 갈래 중 고르신 것 — 지나간 감정도 화면에 남는다)
- **그림은 그 대사와 함께 나온다.** 표식은 문단 맨 앞에 오므로 "표식은 왔는데 대사는 아직"인
  순간이 반드시 생긴다. 그래서 `splitSpeech` 는 **글자 없는 토막을 내보내지 않는다.**
- **동그라미가 아니라 둥근 네모.** 원본이 1024×1024 정사각형이라 동그랗게 오리면 잘린다.
- **한 변 120px.** 200으로 뒀더니 우측 패널이 열렸을 때 말풍선이 199px까지 눌렸다
  (120이면 289px). 실측해서 정한 값이다.
- **`next/image` 최적화를 켜 둔다.** 원본 1.6MB → 220KB. `unoptimized` 를 다시 붙이지 말 것.
- 캐릭터마다 감정 이름이 다르다. **지원(jiwon)만 파일명에 번호가 없다** (`감정.png`).

## 관리자 페이지

- **탭 셋** — 프롬프트(대화구조·대본 10·흐름 9) · 이용내역 · 점검.
  껍데기는 `admin/page.tsx` 의 `Console`, 알맹이는 탭마다 다른 컴포넌트다.
- **이용내역은 `invention_notes` 를 읽는다.** 그 표는 RLS가 켜져 있고 정책이 없어서
  브라우저 키로는 못 읽는다 — `api/admin/notes` 가 서버 전용 키로 읽는 **유일한 창구**다.
- **막힘 신호는 프로그램이 잰 값으로만 판정한다** (`lib/notes/summary.ts`).
  반려 3회 이상 · 한 단계 30분 이상 · 마지막 활동 2일 이상.
  임계값을 고치면 `tests/usage.test.ts` 가 함께 움직인다.
- 이 요약은 **3.0 교사 대시보드가 그대로 쓸 값**이다 (ECOSYSTEM 3.3).
  화면이 아니라 순수 함수에 둔 이유다.

## 랜딩페이지

- **`/` 는 랜딩, 대화는 `/chat`.** 세션 저장 자리 이름은 `lib/session.ts` 의
  `SESSION_STORAGE_KEY` 하나로 모았다.
- **1.0 디자인 규칙을 따른다** — 무지개 워드마크(`components/Wordmark.tsx`), `#0066FF`,
  둥근 필 버튼, 흐릿한 그라데이션. 1.0 색 토큰을 `globals.css` 에 옮겨 뒀다(고치지 말 것).
- **스크롤로 내려 읽는 소개 페이지가 아니다.** 한 화면에 담는다.
  세로 여백과 얼굴 크기가 창 크기에 따라 줄어든다(`[@media(min-height:760px)]` 등).
  실측: 1440×900 · 1366×640 · 768×1024 스크롤 0. (375×812 휴대폰만 가운데가 스크롤된다)

## 화면

- **카드 열 수는 `@container` 로 패널 폭 기준.** 화면 기준으로 잡으면 채팅이 36%를
  쓰기 때문에 넓은 모니터에서도 카드가 짓눌린다. 1440→4열, 1024→3열, 그 아래 2열.
- **상세 모달은 우측 패널 안에만 얹힌다.** 화면 전체를 덮으면 읽던 말풍선이 가려진다.
- **선배 발명의 특허 검색은 그 발명의 상세 모달 "안에서" 열린다** (1.0과 같은 구조).
  이 검색은 '구경'이라 5단계 기록(`session.patent`)을 덮지 않는다.
- **태그 색값(HEX)은 1.0과 한 글자도 다르지 않게 유지할 것.**

## 5단계 특허 검색 — IdeaPrism의 핵심 흐름

- **학생은 IPC 분류를 고를 줄 모른다.** 그래서 자기 아이디어와 비슷한 **선배 발명을
  먼저 골라, 그 발명의 IPC와 정리된 키워드를 빌려 기초 검색식을 만든다.**
  `generate_kipris_query` 는 `basedOnInventionId` 가 **필수**이고, 없으면 코드가 막는다
  (지침만으로는 안 지켜졌다 — 실측으로 분류 없이 1만 건 넘게 나왔다).
- **AI가 IPC를 직접 고르게 하지 말 것.** 한 번 그렇게 만들었다가 되돌렸다.
- **검색식에 처음 넣는 갈래는 대상(O)·해결수단(S) 둘뿐** (`formula.ts` 의 `DEFAULT_GROUPS`).
  다섯을 다 곱하면 0건이 되는 일이 잦다. 나머지 낱말은 화면 5칸에 꺼진 채 남아
  학생이 켜면 즉시 좁혀진다.
- **`search_kipris` 는 검색식을 인자로 받지 않는다.** AI가 문자열을 건네면 화면에 보이는
  검색식과 실제 조회가 어긋난다.
- **1.0의 검색식 정리는 따라 하지 않는다.** 1.0은 KIPRIS로 보내기 직전 `+`·`*`·괄호를
  공백으로 바꿔, 화면엔 "+ 는 또는"이라 적고 실제로는 전부 '그리고'로 검색한다.
  실측 1,657건 vs 정리 후 68건.

## 대화 주도권

- **0~4단계는 학생이 우측 창을 직접 조작한다.** 처음 한 번만 띄워 주고,
  그다음엔 조작법을 알려 준 뒤 넘긴다. 막혔다고 할 때만 거든다.
  (학생용 검색창 + `/api/search` 가 그래서 있다)
- **5단계는 불려 온 탐정이 주도적으로 찾는다.** "이렇다는 거지 → 이렇게 찾아볼게" 하고
  예고한 뒤 도구를 부른다.
- **화법 지침은 `flow/공통규칙.md` 에 넣어야 걸린다.** 공통규칙은 시스템 프롬프트로
  들어가고 단계 지침은 턴 브리핑이라, 단계 파일에만 적으면 잘 안 지켜진다.

## 되돌아가기

- `quest.furthestStage`(가 본 가장 먼 단계) + `quest.history`(고치기 전 기록).
- 지나온 단계만 눌린다. 안 가 본 단계로 건너뛰는 것은 여전히 막는다.
- **최종본이 발명노트 본문, 밀려난 것은 아래 접이식 '고치기 전 기록'**
  (대표님 방침: "최종을 기준으로 기록해. 시행착오도 의미가 있으니 참고로써 기록").

## 프롬프트·설정 저장

- **파일/코드 = 공장 초기값, `prompt_overrides` 표 = 고친 값(우선).**
  배포한 서버는 파일이 읽기 전용이라 파일에 못 쓰고, 덮어쓰면 되돌릴 수 없다.
  설정값(`kind="config"`, 예: 대화구조)은 파일 대신 **코드가 기본값**을 준다.
- **잠금이 기본값.** `ADMIN_PASSWORD` 가 없으면 관리자 페이지가 아예 안 열린다.
- `personas.ts` 는 순수 함수만 남기고 읽기는 `prompts/persona.ts` 로 분리했다 —
  **회귀 테스트가 `personas.ts` 를 불러 쓰는데 `server-only` 를 들이면 테스트가 깨진다.**
- `personas/`·`flow/` 는 경로를 코드에서 조립해 읽으므로 `next.config.ts` 의
  `outputFileTracingIncludes` 에 `/**` 로 잡아 뒀다. **이걸 빼면 배포본에서 대본을 못 읽어
  채팅이 죽는다.**

---

# 함정 모음 (실제로 겪은 것)

- **Supabase에서 `head: true` 로 개수만 세는 조회는 표가 아예 없어도 오류를 안 준다.**
  그래서 `/api/health` 가 없는 표를 두고 "준비됨"이라 초록으로 보고했다.
  행을 실제로 받아 보게 고쳤다. 새 점검 항목을 만들 때 또 밟기 쉽다.
- **Vercel 프로젝트의 Framework Preset이 "Other"면** 빌드는 성공하는데 `public/` 만
  배포되고 앱 전체가 404가 난다. `nextjs` 로 맞춰야 한다.
- **검색어가 너무 넓으면 조회가 시간 초과된다.** `우산` 한 낱말은 14,461건을 훑다
  초과했다(`우산 보관`·`빗물`은 정상).
- **검색 결과 500건은 새로고침하면 사라진다** — 브라우저 메모리에만 두기 때문. 의도된 동작.
- **`npm run lint` 는 effect 안에서 setState 하는 것을 오류로 막는다.** 억누르지 말고
  구조를 바꿀 것. 두 갈래로 해결해 왔다.
  - 바깥 저장소(sessionStorage)를 읽는 것이면 `useSyncExternalStore` (랜딩 "이어서 하기").
    그릴 때마다 불리므로 값을 캐시해 둘 것.
  - 서버에서 받아 오는 것이면 **상태 변경을 전부 비동기 콜백 안으로**
    (`.then()`/`.finally()`). 관리자 세 화면이 다 이 방식이다.
- **PowerShell로 한글이 든 파일을 일괄 치환하지 말 것.** Windows PowerShell 5.1의
  `Get-Content -Raw` 는 UTF-8 파일을 시스템 코드페이지로 읽어 **한글이 통째로 깨진다.**
  실제로 `useChat.ts` 를 그렇게 날려 `git checkout` 으로 되살렸다.
  이름 바꾸기·문자열 치환은 반드시 편집 도구(Edit)로 할 것.
  한글 파일 이름 바꾸기는 **Bash 도구의 `git mv`** 가 안전하다.
- **커밋 메시지에 한글을 넣을 때 PowerShell here-string(`@'…'@`)이 깨진다.**
  메시지를 파일로 쓴 뒤 `git commit -F 파일` 로 넣을 것.
- **브라우저 콘솔 오류는 탭에 계속 쌓인다.** 코드를 고쳐도 옛 오류가 그대로 보여서
  "아직 안 고쳐졌나?" 하고 헤매기 쉽다. **새 탭을 열어 확인할 것.**
- **도구가 턴 도중에 상태를 바꾸면 시스템 프롬프트는 이미 굳어 있다.**
  `call_expert` 로 부른 손님이 남의 얼굴로 말하던 이유다.
  `route.ts` 의 `liveVoices` 처럼 **턴 안에서 갱신되는 값**이 따로 필요하다.

---

# 개발 환경 메모

- **PowerShell 실행 정책 때문에 `npm run dev` 가 막힌다.** `npm.cmd run dev` 를 쓸 것.
  (Claude Code 의 preview 도구는 `.claude/launch.json` 이 `npm.cmd` 로 잡혀 있어 그냥 된다)
- 환경변수를 고치면 **개발 서버를 껐다 켜야** 반영된다.
- 한글이 든 요청을 `curl -d '...'` 로 인라인 전달하면 **인코딩이 깨진다.**
  파일로 쓴 뒤 `--data-binary @파일` 로 보낼 것.
- 점검: `/api/health` — 키 값은 안 보이고 준비 여부만 나온다. `/admin` 점검 탭에서도 본다.
- 명령: `npm run dev` / `npm test` (133개) / `npm run typecheck` / `npm run lint` / `npm run build`
- **`git push` 는 된다.** (예전 메모에 "AI가 못 한다"고 적혀 있었으나 2026-08-12 세션에서
  실제로 성공했다. Vercel 이 push 를 받아 20초쯤 뒤 자동 배포한다)
- **Vercel 배포 상태는 MCP 도구로 확인한다** — `list_deployments` / `get_deployment`,
  team `team_zhknqZ7xmOH9yWRridz05D0L`, project `ideaprism-chatbot`.
- **AI는 비밀번호를 다루지 않는다.** 관리자 로그인 뒤 화면은 대표님만 확인할 수 있다.

# 대표님과 일하는 방식

- 호칭은 **"대표님"**. 비개발자이므로 기술은 비유로 설명한다.
- 각 단계 완료 시 브리핑 → 승인 후 다음 단계.
- **범위를 과하게 넓히지 말 것.** 한 번 "복잡하다"는 말을 들었다.
  큰 그림을 제안하되 **지금 할 것은 작게 쪼개서** 내놓는다.
- **검증한 것과 못 한 것을 반드시 나눠서 정직하게 보고한다.**
- 키 값은 AI가 다루지 않는다 (대표님이 직접 입력).
