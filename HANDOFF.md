# 인수인계 — 다음 세션에서 이어서 할 일

**갱신: 2026-08-11** · 새 대화창에서 작업을 이어받기 위한 문서.
[CLAUDE.md](CLAUDE.md) → [PRD.md](PRD.md) → 이 문서 순으로 읽으면 된다.

지금 상태: **P0~P5 + 추가 기능 구현 완료, GitHub·Vercel 배포까지 끝남.**
아래 4가지가 다음 세션의 일이다.

---

# 다음 할 일 (대표님 지시)

## 1. 감정 이미지가 10개인데 사실상 1개만 쓰인다

**지금 구조 (조사해 둔 것 — 다시 파헤칠 필요 없음)**

| 어디 | 무엇 |
|---|---|
| `lib/characters.ts` | 캐릭터당 감정 **10개** 정의. 주소 30장 전수 검증(HTTP 200) 완료 |
| `lib/prompt.ts` `createEmotionParser` | 한 응답에서 `[감정:이름]` 태그를 **여러 개** 뽑는다 (`emotions: string[]`) |
| `api/chat/route.ts` | 뽑은 감정마다 `emotion` SSE 이벤트를 보낸다 |
| `hooks/useChat.ts` | `case "emotion": patch({ emotion })` — **덮어쓴다** |
| `components/MessageList.tsx:69` | 말풍선 하나에 `<CharacterAvatar emotion={message.emotion} />` 하나 |

**즉 파이프라인은 여러 감정을 나르는데, 화면이 말풍선당 하나로 뭉갠다.**
AI가 답변 중간에 감정을 바꿔도 마지막 것만 남는다.

**생각해 볼 갈래 (대표님과 정할 것)**
- 문단마다 감정을 바꿔 아바타를 여러 번 보여줄지
- 말풍선은 하나로 두되 감정이 바뀔 때 아바타만 갈아 끼울지(애니메이션)
- 아니면 AI가 감정을 더 자주·다양하게 쓰도록 `flow/공통규칙.md` 쪽을 손볼지
  (지금 규칙은 "답변은 반드시 감정 태그로 시작한다" — **시작**만 요구한다)

## 2. 랜딩페이지 같은 첫 화면

지금은 `/` 로 들어가면 곧바로 대화가 시작된다(캐릭터가 먼저 말을 건다).
서비스 소개·시작하기가 있는 첫 화면이 필요하다.

- `src/app/page.tsx` 가 지금 바로 `useChat()` 을 띄운다
- 세션은 `sessionStorage` 에 있고 `createSession()` 이 만든다 (`lib/session.ts`)
- 랜딩을 `/` 로 두고 대화를 `/chat` 등으로 옮기는 형태가 자연스럽다

## 3. 여러 발명학습 시나리오를 고르는 맞춤형 플랫폼으로 개편

지금은 **발명 5단계 퀘스트 한 가지**만 있다. 이걸 여러 시나리오 중 하나로 만들어야 한다.

**손댈 곳이 깊다 — 설계부터 대표님과 합의할 것**
- `lib/quest.ts` 의 `STAGES` 가 6단계로 **하드코딩**돼 있다 (단계 수·이름·담당·완료 조건)
- `flow/` 파일 이름도 `0-만남` ~ `5-선행기술조사` 로 고정
- `lib/tools.ts` 의 `STAGE_TOOLS` 도 단계 번호에 묶여 있다
- 세션(`types/chat.ts`)에 "어떤 시나리오인가"가 없다

**절대 흔들면 안 되는 것**: 단계 판정은 코드가 한다(원칙 2). 시나리오를 데이터로
빼더라도 **완료 조건 검증은 프로그램이 쥐고 있어야 한다.** 시나리오 정의에
"검증 규칙"을 어떻게 담을지가 이 작업의 핵심 난제다.

## 4. 제대로 된 관리자 페이지

지금 `/admin` 은 **프롬프트 편집만** 된다(캐릭터 대본 3 + 대화 흐름 9).
필요한 것: 페르소나 관리, 시나리오 관리, 설정, 이용내역 조회.

- 지금 구조는 `src/app/admin/page.tsx` 한 장 + `api/admin/*`
- 프롬프트 저장은 `lib/prompts/store.ts` (파일=공장초기값, DB=고친 값)
- **이용내역**은 `invention_notes` 표에 이미 쌓이고 있다
  (세션별 단계 기록·체류시간·재시도·토큰 사용량 — `lib/notes/payload.ts` 참조)
  조회 화면이 없을 뿐이다
- 화면이 늘어나므로 `/admin` 을 탭 구조로 나눌 때가 됐다

---

# 지금 상태

## 배포 (2026-08-11 확인)

| | |
|---|---|
| GitHub | https://github.com/ideaprism/ideaprism_chatbot (**공개**, 대표님 판단으로 그대로 진행) |
| Vercel | https://ideaprism-chatbot.vercel.app · 프로젝트 `ideaprism-chatbot` |
| 배포 커밋 | `d300dfc` — 로컬·GitHub·Vercel 동기화됨 |

**배포본에서 실제로 확인한 것**: 첫 인사 스트리밍 → 0단계 완료 → 배턴터치 →
AI 검색 → 학생 직접 검색(156건·4열) → 상세 모달(대화창 안 가림) →
모달 안 특허 검색(KIPRIS 134건, 쪽 넘기기) → 관리자 잠금. 런타임 오류 0건.

**주의할 점**
- **사이트가 인증 없이 열린다** (`/`, `/admin`, `/api/health` 모두 200).
  Vercel 보호가 `all_except_custom_domains` 라 프로덕션 주소엔 안 걸린다.
  주소를 아는 사람은 누구나 대화할 수 있고 **AI 비용이 대표님 계정에서 나간다.**
  → 콘솔 월 한도로 막거나, Deployment Protection을 Standard로 올릴 것.
- **Claude 키만 비어 있다** (`ANTHROPIC_API_KEY`). OpenAI·Gemini는 실제로 확인됨.
- **관리자 저장 성공 경로는 미확인** — 접근 코드를 대표님이 바꾸셔서 AI가 못 들어간다.
  `배포주소/admin` 에서 아무 글이나 고쳐 저장해 보시면 확인된다.
- 로컬 `.env.local` 의 `ADMIN_PASSWORD` 는 아직 확인용 임시값(`ideaprism-admin`)이다.

## 만들어진 것

| 단계 | 내용 |
|---|---|
| P0~P5 | 골격·검색 콘솔·배턴터치·발명노트·특허 연결·다듬기 (전부 완료) |
| 추가 | 모델 3사 비교 (Claude·OpenAI·Gemini) |
| 추가 | 대화 흐름을 `flow/` 파일로 분리 |
| 추가 | 우측 패널을 1.0 화면에 맞춤 (갤러리·상세보기·특허검색) |
| 추가 | 앞 단계 되돌아가기 + 시행착오 보존 |
| 추가 | 학생이 우측 창을 직접 조작 (0~4단계) |
| 추가 | 관리자 페이지 (프롬프트 편집) |

회귀 테스트 **87개**. `npm test` / `npm run typecheck` / `npm run lint` / `npm run build` 전부 통과.

## 폴더 구조

```
personas/     캐릭터 대본 (공장 초기값) — /admin 에서도 고침
flow/         대화 흐름 지침 (공장 초기값, 9개)
supabase/     invention_notes · prompt_overrides DDL (둘 다 적용됨)
tests/        회귀 테스트 87개
src/app/
  page.tsx          채팅 + 2패널
  admin/page.tsx    관리자 (프롬프트 편집)
  api/              chat · search · kipris · ipc · invention-keywords · admin/* · health · providers
src/lib/
  admin/auth.ts     관리자 잠금 (접근 코드·서명 쿠키)
  prompts/store.ts  프롬프트 저장소 (파일 기본값 + DB 덮어쓰기)
  quest.ts          퀘스트 상태기계 + 승급 검증   ← 단계 판정은 여기만
  tools.ts          도구 8종 명세
  tool-handlers.ts  도구 실행기
  prompt.ts         프롬프트 조립 + 표식 파서 + 대화 압축
  characters.ts     캐릭터 + 감정 이미지 매핑
  personas.ts       페르소나 정제 (순수 함수 — server-only 들이지 말 것)
  tag-styles.ts     태그 색·아이콘 (1.0과 같은 색값)
  search/ kipris/ notes/ ai/
```

---

# 절대 깨뜨리면 안 되는 것 (PRD 아키텍처 원칙)

1. **AI는 도구 8종만 누른다.** 미구현 도구는 AI에게 전달조차 하지 않는다.
2. **단계 판정은 코드가 한다.** `complete_stage` + `quest.ts` 검증을 통과해야만 승급.
   산출물의 "모양"만 보지 말고 **프로그램이 실제로 한 일**(`StageEvidence`)로 판정할 것 —
   AI는 하지 않은 조사를 그럴듯하게 적어 낸다(5단계에서 실제로 겪음).
3. **감정은 이름만.** AI는 `[감정:이름]` 표식만 쓰고 주소 조립은 `characters.ts`.
4. **숫자는 프로그램이 센다.** AI가 목록에 없는 값을 지어내면 버리고 무엇을 버렸는지 알린다.
5. **대화 도중 모델 회사 교체 금지.**
6. **개인정보 미수집.** 별명만 받는다.

## 세 회사가 공통으로 요구하는 것

도구를 호출한 모델 턴을 되돌려 줄 때 **회사마다 이름만 다른 "생각 흔적"을 살려야 한다.**
안 그러면 다음 턴이 400으로 막힌다. 지금은 `AiMessage.raw` 로 셋 다 해결돼 있다.

| 회사 | 살려야 하는 것 |
|---|---|
| Claude | thinking 블록 |
| OpenAI | reasoning 항목 (그래서 Chat Completions 대신 **Responses API**) |
| Gemini | `thoughtSignature` |

---

# 굳힌 결정 (다시 고민하지 말 것)

## 화면

- **카드 열 수는 `@container` 로 패널 폭 기준.** 화면 기준으로 잡으면 채팅이 36%를
  쓰기 때문에 넓은 모니터에서도 카드가 짓눌린다. 1440→4열, 1024→3열, 그 아래 2열.
- **상세 모달은 우측 패널 안에만 얹힌다.** 화면 전체를 덮으면 읽던 말풍선이 가려진다.
- **선배 발명의 특허 검색은 그 발명의 상세 모달 "안에서" 열린다** (1.0과 같은 구조).
  좌측 위 썸네일을 누르면 상세로 돌아온다. 이 검색은 '구경'이라
  5단계 선행기술조사 기록(`session.patent`)을 덮지 않는다.
- **태그 색값(HEX)은 1.0과 한 글자도 다르지 않게 유지할 것.**

## 5단계 특허 검색 — IdeaPrism의 핵심 흐름

- **학생은 IPC 분류를 고를 줄 모른다.** 그래서 자기 아이디어와 비슷한 **선배 발명을
  먼저 골라, 그 발명의 IPC와 정리된 키워드를 빌려 기초 검색식을 만든다.**
  `generate_kipris_query` 는 `basedOnInventionId` 가 **필수**이고, 없으면 코드가 막는다
  (지침만으로는 안 지켜졌다 — 실측으로 분류 없이 1만 건 넘게 나왔다).
- **AI가 IPC를 직접 고르게 하지 말 것.** 한 번 그렇게 만들었다가 되돌렸다.
- **검색식에 처음 넣는 갈래는 대상(O)·해결수단(S) 둘뿐** (`formula.ts` 의 `DEFAULT_GROUPS`).
  다섯을 다 곱하면 0건이 되는 일이 잦다. 나머지 낱말은 화면 5칸에 꺼진 채 남아
  학생이 켜면 즉시 좁혀진다.
- **`search_kipris` 는 검색식을 인자로 받지 않는다.** AI가 문자열을 건네면 화면에 보이는
  검색식과 실제 조회가 어긋난다.
- **1.0의 검색식 정리는 따라 하지 않는다.** 1.0은 KIPRIS로 보내기 직전 `+`·`*`·괄호를
  공백으로 바꿔, 화면엔 "+ 는 또는"이라 적고 실제로는 전부 '그리고'로 검색한다.
  실측 1,657건 vs 정리 후 68건.

## 대화 주도권

- **0~4단계(선생님·지유)는 학생이 우측 창을 직접 조작한다.** 처음 한 번만 띄워 주고,
  그다음엔 조작법을 알려 준 뒤 넘긴다. 막혔다고 할 때만 거든다.
  (학생용 검색창 + `/api/search` 가 그래서 있다)
- **5단계 탐정만 주도적으로 찾는다.** "이렇다는 거지 → 이렇게 찾아볼게" 하고
  예고한 뒤 도구를 부른다.
- **화법 지침은 `flow/공통규칙.md` 에 넣어야 걸린다.** 공통규칙은 시스템 프롬프트로
  들어가고 단계 지침은 턴 브리핑이라, 단계 파일에만 적으면 잘 안 지켜진다.

## 되돌아가기

- `quest.furthestStage`(가 본 가장 먼 단계) + `quest.history`(고치기 전 기록).
- 지나온 단계만 눌린다. 안 가 본 단계로 건너뛰는 것은 여전히 막는다.
- **최종본이 발명노트 본문, 밀려난 것은 아래 접이식 '고치기 전 기록'**
  (대표님 방침: "최종을 기준으로 기록해. 시행착오도 의미가 있으니 참고로써 기록").

## 프롬프트 저장

- **파일 = 공장 초기값(git), `prompt_overrides` 표 = 고친 값(우선).**
  배포한 서버는 파일이 읽기 전용이라 파일에 못 쓰고, 덮어쓰면 되돌릴 수 없다.
- **잠금이 기본값.** `ADMIN_PASSWORD` 가 없으면 관리자 페이지가 아예 안 열린다.
- `personas.ts` 는 순수 함수만 남기고 읽기는 `prompts/persona.ts` 로 분리했다 —
  **회귀 테스트가 `personas.ts` 를 불러 쓰는데 `server-only` 를 들이면 테스트가 깨진다.**
- `personas/`·`flow/` 는 경로를 코드에서 조립해 읽으므로 `next.config.ts` 의
  `outputFileTracingIncludes` 에 `/**` 로 잡아 뒀다. **이걸 빼면 배포본에서 대본을 못 읽어
  채팅이 죽는다.**

---

# 함정 모음 (실제로 겪은 것)

- **Supabase에서 `head: true` 로 개수만 세는 조회는 표가 아예 없어도 오류를 안 준다.**
  그래서 `/api/health` 가 없는 표를 두고 "준비됨"이라 초록으로 보고했다.
  행을 실제로 받아 보게 고쳤다. 새 점검 항목을 만들 때 또 밟기 쉽다.
- **Vercel 프로젝트의 Framework Preset이 "Other"면** 빌드는 성공하는데 `public/` 만
  배포되고 앱 전체가 404가 난다. `nextjs` 로 맞춰야 한다.
- **검색어가 너무 넓으면 조회가 시간 초과된다.** `우산` 한 낱말은 14,461건을 훑다
  초과했다(`우산 보관`·`빗물`은 정상).
- **검색 결과 500건은 새로고침하면 사라진다** — 브라우저 메모리에만 두기 때문
  (세션 요약만 sessionStorage). 지금은 의도된 동작.
- **`npm run lint` 는 effect 안에서 setState 하는 것을 오류로 막는다.** 억누르지 말고
  구조를 바꿀 것 (값을 바꾸는 곳에서 함께 바꾸거나, 비동기 콜백 안으로 옮기기).

---

# 개발 환경 메모

- **PowerShell 실행 정책 때문에 `npm run dev` 가 막힌다.** `npm.cmd run dev` 를 쓸 것.
- 환경변수를 고치면 **개발 서버를 껐다 켜야** 반영된다.
- 한글이 든 요청을 `curl -d '...'` 로 인라인 전달하면 **인코딩이 깨진다.**
  파일로 쓴 뒤 `--data-binary @파일` 로 보낼 것.
- 점검: `/api/health` — 키 값은 안 보이고 준비 여부만 나온다.
- 명령: `npm run dev` / `npm test` (87개) / `npm run typecheck` / `npm run lint` / `npm run build`
- **`git push` 는 이 환경의 권한 정책에 막혀 AI가 실행할 수 없다.** 대표님이 직접 하신다.

# 대표님과 일하는 방식

- 호칭은 **"대표님"**. 비개발자이므로 기술은 비유로 설명한다.
- 각 단계 완료 시 브리핑 → 승인 후 다음 단계.
- **로컬 커밋은 자유, push·배포는 반드시 승인 후.**
- 검증한 것과 못 한 것을 반드시 나눠서 정직하게 보고한다.
- 키 값은 AI가 다루지 않는다 (대표님이 직접 입력).
